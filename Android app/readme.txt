This application works as a firewall filter, which cost the user less data stream while using it. 
Firewall filter is able to manage individual softwares, installed in the Android system, to chose either
wifi only or data stream only to access to the Internet.